import 'package:flutter/material.dart';
import 'add_cart.dart';

class CartScreenWrapper extends StatelessWidget {
  const CartScreenWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const AddCartPage();
  }
}
